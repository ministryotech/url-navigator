{
  "name": "url-navigator",
  "version": "2.0.0",
  "author": "Keith Jackson",
  "license": "MIT",
  "private": false,
  "keywords": [
    "window",
    "global",
    "wrapper",
    "url"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/ministryotech/url-navigator.git"
  },
  "devDependencies": {
    "eslint": "^8.56.0",
    "eslint-config-defaults": "^9.0.0"
  },
  "dependencies": {},
  "scripts": {
    "warn-win": "echo The Windows scripts have not been fully tested and may need an update...",
    "ensure-dir": "mkdir ./deploy/build",
    "copy-module": "cp url-navigator.js ./deploy/build/index.js",
    "copy-readme": "cp README.md ./deploy/build/",
    "copy-package-json": "cp package.json ./deploy/build/",
    "copy-module-win": "xcopy url-navigator.js ./deploy/build/index.js",
    "copy-readme-win": "xcopy README.md ./deploy/build/",
    "copy-package-json-win": "xcopy package.json ./deploy/build/",
    "build": "npm run clean && npm run ensure-dir && npm run copy-module && npm run copy-readme && npm run copy-package-json",
    "build-win": "npm run warn-win && npm run clean-win && npm run ensure-dir && npm run copy-module-win && npm run copy-readme-win && npm run copy-package-json-win",
    "clean": "rm -rf ./deploy/build",
    "clean-win": "rmdir ./deploy/build /f",
    "lint": "eslint --fix",
    "publish-to-npm": "npm publish deploy/build/",
    "build-tar-for-github": "tar -czvf url-navigator.tar.gz deploy/build/",
    "release": "npm run lint && npm run build && npm run publish-to-npm && npm run build-tar-for-github",
    "release-win": "npm run lint && npm run build-win && npm run publish-to-npm"
  }
}
